#spring:
#  application:
#    name: zone-service
#  cloud:
#    config:
#      import: "optional:configserver:http://localhost:8888"
spring:
  application:
    name: zone-service

  config:
    import: optional:configserver:http://localhost:8888

server:
  port: 8081