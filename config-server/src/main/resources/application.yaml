server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: "file:///D:/AGMS - Microservice/config-repo"
          default-label: main