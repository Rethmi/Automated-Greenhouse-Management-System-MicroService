#spring:
#  application:
#    name: automation-service
#  cloud:
#    config:
#      import: "optional:configserver:http://localhost:8888"
spring:
  application:
    name: automation-service
  server:
    port: 8083
  config:
    import: optional:configserver:http://localhost:8888